import ProfilePageClient from "@/components/profile/ProfilePageClient";

/**
 * Renders the /profile/orders page.
 *
 * @returns The page UI.
 */
export default function ProfileOrdersPage() {
  return <ProfilePageClient initialSection="orders" />;
}
